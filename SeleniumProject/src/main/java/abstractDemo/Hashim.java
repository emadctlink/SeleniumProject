package abstractDemo;

public class Hashim extends MobileUser {

	//Both call() and sendmassage() methods internally connected by Override
	
		@Override
		void sendmassage() {
			System.out.println("Hashim is fully abstract and inherit from MobileUser ");
}
}