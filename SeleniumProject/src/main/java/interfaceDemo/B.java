package interfaceDemo;

public interface B {
	
	void play();
	public void chess();
	
	//Inside any  interface- method does not has body, code, logic
	//So method doesn't has body or implementation or logic
	//that means these are fully abstract.
	//to execute go to implementation class C
	
	// in interface we can only declare method not full method signature
	
}
